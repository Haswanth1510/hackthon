# Stylic.AI — Python Dependencies
# Install: pip install -r requirements.txt

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
gunicorn>=22.0.0           # production WSGI manager used by Render

# Data validation (including email-validator for EmailStr)
pydantic[email]>=2.7.0
email-validator>=2.1.0

# HTTP client (for Gemini, Groq, Roboflow APIs)
httpx>=0.27.0

# File upload support
python-multipart>=0.0.9

# Image processing
pillow>=10.3.0

# Numerical processing
numpy>=1.26.0

# Environment variable loading
python-dotenv>=1.0.1
